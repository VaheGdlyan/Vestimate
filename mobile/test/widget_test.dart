import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Vestimate app smoke test', (WidgetTester tester) async {
    // Placeholder test — full widget tests in test/features/
    expect(true, isTrue);
  });
}
